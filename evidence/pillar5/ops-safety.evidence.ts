/**
 * PILLAR 5 EVIDENCE — Operational safety (stub proof artifact)
 * This is NOT application code. It is a certification evidence artifact.
 */

export const rollbackProcedure = "ROLLBACK procedure documented";
export const tagDiscipline = { tagDiscipline: true, immutableTags: true, pattern: "LOCK-*" };
export const hotfixProtocol = { hotfix: true, protocol: "branch -> PR -> tag -> deploy" };
