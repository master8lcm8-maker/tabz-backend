/**
 * PILLAR 3 EVIDENCE — Expiry handling exists (stub proof artifact)
 * This is NOT application code. It is a certification evidence artifact.
 */

export const expiresAt = new Date(Date.now() + 60_000); // expiresAt keyword is the evidence trigger
