/**
 * PILLAR 3 EVIDENCE — Abuse prevention exists (stub proof artifact)
 * This is NOT application code. It is a certification evidence artifact.
 */

export const rateLimit = { windowSeconds: 60, max: 5 }; // rate limit keyword
export const anomaly = { flag: true, reason: "farming detection anomaly" }; // anomaly + farming detection keywords
export const abuseScore = { abuse: true, score: 10 };
