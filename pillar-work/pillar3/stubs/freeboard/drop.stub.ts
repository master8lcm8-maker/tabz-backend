/**
 * FreeBoard Drop (stub)
 * Not app code. Placeholder blueprint.
 */
export type Drop = {
  id: number;
  venueId: number;
  title: string;
  qtyTotal: number;
  qtyRemaining: number;
  expiresAt: string;
  createdAt: string;
};
