/**
 * PILLAR 5 — Splash Reference (Slice A / Add-only)
 * Placeholder reference for splash assets.
 */
export const splashRef = {
  required: true,
  notes: "Provide splash screen images + background color; wire into Expo config later (deferred).",
};
