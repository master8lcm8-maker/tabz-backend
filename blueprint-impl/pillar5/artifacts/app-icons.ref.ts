/**
 * PILLAR 5 — App Icons Reference (Slice A / Add-only)
 * Placeholder reference for store submission assets.
 */
export const appIconsRef = {
  required: true,
  notes: "Provide iOS AppIcon + Android mipmaps. Store in tabz-mobile assets later (wiring deferred).",
};
